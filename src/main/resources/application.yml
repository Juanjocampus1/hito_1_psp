server:
  port: 8080

spring:
  application:
    name: HitoPSP
    datasource:
      url: jdbc:mysql://localhost:3306/ecomerch
      username: root
      password: curso
      driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
      hibernate:
        ddl-auto: create-drop
      show-sql: true
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MySQLDialect